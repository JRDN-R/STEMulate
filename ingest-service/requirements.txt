Flask==3.1.2
google-cloud-firestore==2.21.0
gunicorn==23.0.0
requests==2.34.2
spotdl==4.5.2
# Do not add yt-dlp's `pin` extra: it pins websockets 16 while spotDL 4.5.2
# requires websockets 14.x. The yt-dlp package itself remains exactly pinned.
# `default` supplies the EJS challenge scripts, `deno` supplies their runtime,
# and curl-cffi adds yt-dlp's recommended TLS impersonation support.
yt-dlp[default,deno,curl-cffi]==2026.7.4
# The pinned plugin talks only to the loopback PO-token sidecar configured by
# YOUTUBE_POT_PROVIDER_URL. The worker supplies empty HOME/XDG directories and
# disables Python user-site packages for every downloader process.
bgutil-ytdlp-pot-provider==1.3.1
